name: Backend

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      # https://github.com/ASzc/change-string-case-action
      - uses: actions/checkout@v2
      - id: nwo
        uses: ASzc/change-string-case-action@v1
        with:
          string: ${{ github.repository }}
      - name: Build containers
        run: script/build-backend.sh
        env:
          DOCKER_REGISTRY: containers.pkg.github.com
          REPO_PATH: ${{ steps.nwo.outputs.lowercase }}
      - name: Push to GPR
        # if: ${{ github.event_name == 'push' && github.ref == 'refs/heads/main' }}
        run: |
          echo "${{ secrets.GITHUB_TOKEN }}" | docker login containers.pkg.github.com -u ${{github.actor}} --password-stdin
          docker push containers.pkg.github.com/${{ steps.nwo.outputs.lowercase }}/backend:latest
      # - uses: akhileshns/heroku-deploy@v3.5.7 # This is the action
      #   with:
      #     heroku_api_key: ${{secrets.HEROKU_API_KEY}}
      #     heroku_app_name: "carbonchat" #Must be unique in Heroku
      #     heroku_email: "${{secrets.HEROKU_EMAIL}}"
      #     appdir: backend
      #     usedocker: true


# docker login --username=_ --password=$(heroku auth:token) registry.heroku.com
# docker tag <image> registry.heroku.com/carbonchat/backend:latest
# docker push registry.heroku.com/carbonchat/backend:latest